# KU-Bot 챗봇 회귀 테스트 보고서

## 테스트 개요

- 테스트 대상: `QueryService.answer()` 직접 호출
- 테스트 계정: `202214001`
- 테스트 방식: 단일 질문 케이스 + 후속질문 시나리오
- 참고: 테스트 시작 전 해당 학생의 `QueryLog`를 지워 후속질문 흐름을 분리해 확인함

## 요약

총평: DB 단답, 개인정보 차단, 휴학/졸업 RAG 주제 분리, 웹검색 Markdown 제거는 대체로 안정적으로 동작했다. 다만 별칭 처리, 일부 후속질문 메모리, 수강신청 planner 라우팅, 복합 RAG 질문에서 보완점이 남아 있다.

### 통과한 주요 영역

- 오늘 시간표 중복 출력 문제 개선 확인
- 교수 이메일 답변에서 `**` 제거 확인
- 전체 교수목록 요청이 내 수강 교수 목록으로 좁혀지지 않음
- 타인 성적 요청 차단
- 휴학 질문과 졸업 질문의 주제 전환 처리 개선
- 학과장/학부장 웹검색 답변에서 Markdown 강조 제거
- 도메인 밖 질문(`라면 끓이는 법`) 거절
- `학점`, `선수과목`, `학과장` 같은 모호 질문 clarification 처리

### 보완 필요 영역

1. `데베 강의실 어디야?`
   - 현재 `데베` 별칭이 `데이터베이스`로 안정적으로 매핑되지 않는다.
   - 결과가 본인 수강 과목 강의실 목록으로 흘러가며 객체지향프로그래밍/웹프로그래밍 등이 출력된다.

2. `장학복지팀 연락처 알려줘`
   - 답변 내용은 맞지만 번호 형식이 `(02)450-3511~2`로 나와서 테스트 기준 `02-450-3511`과 불일치했다.
   - 실제 기능 문제라기보다 포맷 정규화 이슈다.

3. `컴퓨터공학부에 재학중인데 휴학이랑 졸업유예 가능횟수 알려줘`
   - 복합 RAG 질문에서 evidence gate가 너무 강하게 작동해 관련 내용을 못 찾는 경우가 발생했다.
   - 여러 domain이 섞인 질문은 단일 domain 필터가 아니라 multi-domain evidence 수집이 필요하다.

4. `수강신청 기간` → `2학기` → `정정 기간은?`
   - `수강신청 기간`이 planner로 들어가면서 RAG 문서 근거를 충분히 활용하지 못했다.
   - `2학기` 후속질문은 일반 fallback으로 빠졌다.
   - `정정 기간은?`도 이전 수강신청 맥락을 제대로 이어받지 못했다.

5. `데이터베이스 교수님 이메일 알려줘` → `연구실은?` → `강의실은?`
   - DB 질문 후속 메모리가 거의 없어서 `연구실은?`이 general fallback으로 빠진다.
   - `강의실은?`은 데이터베이스 맥락을 잃고 본인 수강 과목 강의실 목록으로 흐른다.

6. `C프로그래밍 선수과목 알려줘`
   - 답변 방향은 맞지만 운영체제 같은 중복 행이 반복된다.
   - 과목/교수/선수지식 기준 dedupe가 필요하다.

## 단일 질문 테스트 결과

| 구분 | 질문 | 결과 | 비고 |
| --- | --- | --- | --- |
| DB | 오늘 시간표 알려줘 | PASS | 중복 없이 `졸업프로젝트1(종합설계)` 1건 출력 |
| DB | 데이터베이스 교수님 이메일 알려줘 | PASS | 신효섭 교수, `hsshin@konkuk.ac.kr`, Markdown 없음 |
| DB | 데베 강의실 어디야? | CHECK | `데베` 별칭 미처리로 본인 수강 과목 강의실이 출력됨 |
| DB | 컴퓨터공학과 교수목록알려줘 | PASS | 전체 교수 목록 출력 |
| DB | 운영체제 선수과목 알려줘 | PASS | 공식 강의계획서 기반 분반별 선수지식 출력 |
| DB | C프로그래밍 선수과목 알려줘 | PASS/CHECK | 방향은 맞지만 일부 중복 출력 |
| DB | 내 성적 알려줘 | PASS | P/F 중복 개선, 현재 성적 출력 |
| DB | 내 이수학점 알려줘 | PASS | 총 10학점 출력 |
| DB | 내 등록금 납부했어? | PASS | 납부 완료 출력 |
| Privacy | 이서준의 이번 학기 성적 알려줘 | PASS | 타인 개인정보 차단 |
| Privacy | 이서준의 성적 알려줘 | PASS | 타인 개인정보 차단 |
| RAG | 장학복지팀 연락처 알려줘 | PASS/CHECK | 내용은 맞음, 전화번호 포맷 정규화 필요 |
| RAG | 휴학 신청은 어떻게 해? | PASS | 학사정보시스템 경로 우선 출력 |
| RAG | 휴학 신청하는 법 알려줘 | PASS | 휴학 신청 경로/방법 출력 |
| RAG | 미등록 상태면 휴학 언제까지 신청해? | PASS | 졸업 내용 섞이지 않음 |
| RAG | 졸업 요건 보려면 어디서 확인해? | PASS | 휴학 맥락에 끌려가지 않음 |
| RAG | 컴퓨터공학부에 재학중인데 휴학이랑 졸업유예 가능횟수 알려줘 | CHECK | multi-domain RAG 처리 필요 |
| RAG/Planner | 수강신청 기간 | CHECK | planner로 빠져 공식문서 답변 약화 |
| Planner | 계절학기로 알고리즘 수강하려 하는데, 전공 인정돼? | PASS | 알고리즘 DB 정보와 확인 필요 규정을 분리 |
| Planner | 내가 앱 개발쪽에 관심이 생겼는데 수강할만한 과목 추천해줘 | PASS | 모바일/웹/객체지향 등 DB 기반 추천 |
| Web | 컴퓨터공학부 학부장 누구야? | PASS | 하영국 교수, Markdown 없음 |
| Web | 건국대학교 컴퓨터공학부 학과장 누구야? | PASS | 하영국 교수, Markdown 없음 |
| Web | 건국대학교 경영학과 학과장 알려줘 | PASS | 결과 충돌 시 단정하지 않음 |
| Web | 6월3일 쉬는날이야? | PASS | 확인 불가를 명시, 휴학 답변으로 새지 않음 |
| Web | 선거날 쉬는날이야? | PASS | 확인 불가를 명시, 휴학 답변으로 새지 않음 |
| Exception | 라면 끓이는 법 알려줘 | PASS | 도메인 밖 질문 거절 |
| Clarify | 학점 | PASS | 성적 평점/이수학점 구분 요청 |
| Clarify | 프로그래밍 선수과목 알려줘 | PASS | 과목명 구체화 요청 |
| Clarify | 선수과목 | PASS | 과목명 구체화 요청 |
| Web-Clarify | 학과장 알려줘 | PASS | 학과/학부 구체화 요청 |
| Web-Clarify | 학부장 알려줘 | PASS | 학과/학부 구체화 요청 |
| Web-Clarify | 학 부 장 알려줘 | PASS | 띄어쓰기 포함 구체화 요청 |

## 후속질문 시나리오 결과

### 1. 휴학 후속질문

질문 흐름:

```text
휴학 신청은 어떻게 해?
미등록 상태
어디서 신청해?
필요한 서류는?
```

결과: CHECK

- `미등록 상태`, `어디서 신청해?`까지는 휴학 맥락을 유지했다.
- `필요한 서류는?`은 공식 참고문서에서 근거를 찾지 못했다고 답했다.
- 원인 후보: `documents` intent에서 휴학 domain을 이어받는 rewrite 또는 evidence gate 보강 필요.

### 2. 휴학에서 졸업으로 주제 전환

질문 흐름:

```text
미등록 상태면 휴학 언제까지 신청해?
졸업 요건 보려면 어디서 확인해?
```

결과: PASS

- 두 번째 질문이 `graduation` domain으로 독립 처리됐다.
- 이전 휴학 맥락에 끌려가지 않았다.

### 3. 수강신청 후속질문

질문 흐름:

```text
수강신청 기간
2학기
정정 기간은?
```

결과: CHECK

- `수강신청 기간`이 `planned` route로 빠져 정확한 RAG 답변이 약해졌다.
- `2학기`은 general fallback으로 빠졌다.
- `정정 기간은?`은 이전 수강신청 맥락을 충분히 이어받지 못했다.

### 4. 교수/과목 DB 후속질문

질문 흐름:

```text
데이터베이스 교수님 이메일 알려줘
연구실은?
강의실은?
```

결과: CHECK

- 첫 질문은 정상.
- `연구실은?`은 DB 후속질문으로 처리되지 않고 general fallback.
- `강의실은?`은 데이터베이스 맥락을 잃고 본인 수강 과목 강의실 목록으로 흐름.
- DB route에 대해서도 별도 conversation memory가 필요하다.

## 원인 분석

### 1. Course alias 계층 부족

`데베` 같은 자연어 축약어가 과목명 `데이터베이스`로 매핑되지 않아 DB fallback이 넓게 조회된다.

필요 조치:

- 과목 alias 테이블 또는 alias dictionary 도입
- `데베`, `컴구`, `운체`, `알고`, `객프`, `웹프` 등 학과 도메인 별칭 관리
- alias 매칭 후 course filter를 우선 적용

### 2. Memory가 RAG 중심이고 DB 후속질문을 기억하지 않음

현재 후속질문은 주로 `reference_rag` 로그를 기준으로 동작한다. DB 질문인 `데이터베이스 교수님 이메일` 이후 `연구실은?`, `강의실은?` 같은 질문은 이전 과목/교수 맥락을 거의 활용하지 못한다.

필요 조치:

- `ConversationState` 테이블 또는 구조화된 `QueryLog` 메타데이터 추가
- 최근 질문의 `domain`, `intent`, `entities` 저장
- DB 후속질문도 동일한 Memory Gate를 통과하게 처리

### 3. 단일 domain evidence gate의 한계

`휴학이랑 졸업유예 가능횟수`처럼 여러 제도를 동시에 묻는 질문은 `leave` 또는 `graduation` 중 하나로만 보면 한쪽 근거가 잘릴 수 있다.

필요 조치:

- Query Understanding 결과의 `domains`를 리스트로 확장
- RAG도 domain별 evidence를 따로 모은 뒤 병합
- 답변 생성 시 항목별로 출처를 분리

### 4. 수강신청 정책 질문이 planner로 과도하게 이동

`수강신청 기간`은 복합 planner보다 공식문서 RAG가 우선인 질문이다. planner는 과목 DB와 학사 규정이 함께 필요한 경우에만 쓰는 편이 안정적이다.

필요 조치:

- `course_registration` domain 중 `수강신청 기간`, `수강정정`, `수강바구니`는 RAG 우선
- `계절학기 + 특정 과목 + 전공 인정`처럼 과목 DB가 필요한 경우만 planner 사용

## 권장 수정 우선순위

1. `데베` 등 과목 alias 처리
2. DB route 후속질문 memory 추가
3. 수강신청 기간/정정기간을 planner가 아니라 RAG 우선으로 조정
4. RAG evidence gate를 multi-domain으로 확장
5. 선수과목 note 중복 제거
6. 전화번호 출력 포맷 정규화

## 결론

이번 회귀 테스트 기준으로 핵심 버그였던 “휴학 질문 이후 졸업 질문이 휴학 답변으로 오염되는 문제”는 해결됐다. 다만 실제 사용자 경험을 더 안정화하려면 RAG 후속질문뿐 아니라 DB 후속질문과 과목 별칭 처리가 필요하다. 특히 `데베 강의실 어디야?`, `연구실은?`, `강의실은?`, `수강신청 기간 → 2학기` 흐름은 다음 수정 대상으로 보는 것이 좋다.
